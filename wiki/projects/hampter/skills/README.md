# hampter Skills

Use this folder for parent-level HAMPTER workflows that apply across multiple HAMPTER subprojects.

Current rules:
- Keep parent-level process here.
- Keep subproject-specific workflow inside each subproject's `skills/` folder.
- Preserve the `hampter -> subprojects -> <name>` hierarchy in the wiki.
