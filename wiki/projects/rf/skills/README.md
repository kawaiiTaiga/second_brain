# rf Skills

Use this folder for parent-level RF/networking workflows that apply across multiple RF subprojects.

Current rules:
- Keep parent-level wireless/networking process here.
- Put codebase- or topology-specific workflow inside each RF subproject's `skills/` folder.
- Separate Linux network bring-up from application-level middleware logic.
- Use placeholders for secrets, private IPs, and personal machine paths.
