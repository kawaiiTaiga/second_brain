# Shared Knowledge

Read this page after [shared/index.md](../index.md). This is the shared knowledge hub.

Use this folder for knowledge that is useful across projects and not owned by a single repository.

## Current State

- No shared deep knowledge pages are registered yet.

## Rules

- Add only facts that are reused across projects.
- If a note matters to only one project, move it into that project's `knowledge/` area.
- When shared knowledge grows, list the deep pages here and keep `shared/index.md` as the higher-level entry point.
